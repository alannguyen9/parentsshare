import { StyleSheet } from "react-native";

const styles=StyleSheet.create({
    inputContainer: {
        borderWidth: 1,
        borderRadius: 5,
        alignItems: "center",
        marginBottom: 10
    },
    inputStyle: {
        padding: 10,
        fontSize: 16
    }
})

export default styles;