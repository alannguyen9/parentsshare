import { StyleSheet } from "react-native";

const styles=StyleSheet.create({

    buttonContainer: {
        padding: 5
    },
    buttonText: {
        fontSize: 16,
        fontWeight: "bold",
        textAlign: "center",
        color: "black"

    }
});

export default styles;